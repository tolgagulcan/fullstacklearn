{
  "name": "git_test",
  "version": "1.0.0",
  "description": "this a test directory to learn git and node",
  "main": "index.html",
  "scripts": {
    "start":"npm run lite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lite":"lite-server"
  },
  "author": "tolga gülcan",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
